public enum DimensionMode {
    _2D,
    _3D
}

public enum Axis {
    X,
    Y,
    Z
}

public enum AxisDirection {
    Up,
    Down,
    Left,
    Right,
    Forward,
    Backwards
}

public enum Bar_Types {
    HORIZONTAL,
    VERTICAL,
    CIRCULAR
}